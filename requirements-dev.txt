# Test and tooling dependencies.
#
# The suite stubs the native/network packages (sounddevice, webrtcvad,
# faster-whisper, ollama), so none of them are needed to run it - no PortAudio,
# no C toolchain, no model downloads, no Ollama server.

-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
pytest-asyncio>=0.24.0
responses>=0.25.0
ruff>=0.6.0

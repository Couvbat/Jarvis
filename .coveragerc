[run]
# The package, not the checkout: an editable install and a bare checkout then
# report the same files, so the number does not move with how it was run.
source_pkgs = jarvis
omit =
    */setup_piper.py

[report]
exclude_lines =
    pragma: no cover
    if __name__ == .__main__.:
    raise NotImplementedError
